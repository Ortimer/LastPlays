<!-- Navigation -->
<nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    {{#link-to 'bgguser'}}<span class="navbar-brand">BGGBuddy V 1.0</span>{{/link-to}}
  </div>
  <!-- /.navbar-header -->

  <div class="navbar-default sidebar" role="navigation">
    <div class="sidebar-nav navbar-collapse">
      <ul class="nav" id="side-menu">
        {{#each singleOption in model.sideMenuOptions}}
          {{#if singleOption.subMenus}}
            <li>
              <a href='#'><i class="fa fa-fw {{singleOption.icon}}"></i> {{singleOption.name}}<span class="fa arrow"></span></a>
              <ul class="nav nav-second-level">
                {{#each subMenuOption in singleOption.subMenus}}
                  <li>
                    {{#link-to subMenuOption.link}}{{subMenuOption.name}}{{/link-to}}
                  </li>
                {{/each}}
              </ul>
            </li>
          {{else}}
            <li>
              {{#link-to singleOption.link}}<i class="fa fa-fw {{singleOption.icon}}"></i> {{singleOption.name}}{{/link-to}}
            </li>
          {{/if}}
        {{/each}}
      </ul>
    </div>
    <!-- /.sidebar-collapse -->
  </div>
  <!-- /.navbar-static-side -->
</nav>
<div style="min-height: 635px;" id="page-wrapper">
{{outlet}}
</div>
